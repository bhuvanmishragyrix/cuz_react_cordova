import React from 'react';
import Preview from '../components/Preview/Preview';

const previewPage = () => {
    return (
        <div className={`mx-3`}>
            <Preview />
        </div>
    );
};

export default previewPage;